import { Link } from "react-router-dom";

function SubsidiarySection({ children }) {
  return (
    <section className="subsidiary__section">
      {children}
    </section>
  );
}

export default SubsidiarySection;
